// import module
// import Session from './model/session';

async function auth(req, res, next) {
	// log all user delete action
	try {
		// try to check something authentication info
	} catch (err) {
		// log
	}
}

export default auth;